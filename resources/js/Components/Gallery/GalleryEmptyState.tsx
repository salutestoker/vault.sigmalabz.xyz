export default function GalleryEmptyState() {
    return (
        <div className="vault-gallery__empty">
            <h2>No media found</h2>
            <p>Try a different search, creator, color, or category filter.</p>
        </div>
    );
}
